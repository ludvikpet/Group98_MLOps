import streamlit as st


st.set_page_config(
    page_title="cleaninbox",
    page_icon="👋",
)
st.title("Welcome to :mailbox_with_no_mail: cleaninbox!")
st.text("We help banks by automatically flagging e-mails such that they are sorted into the correct mail-inbox. Try to test our service, have a look at data-drifting or check inference statistics using the menu to the left!")
